import { MediaListingCounterPipe } from './media-listing-counter.pipe';

describe('MediaListingCounterPipe', () => {
  it('create an instance', () => {
    const pipe = new MediaListingCounterPipe();
    expect(pipe).toBeTruthy();
  });
});
