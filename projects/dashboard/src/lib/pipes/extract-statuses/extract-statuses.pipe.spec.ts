import { ExtractStatusesPipe } from './extract-statuses.pipe';

describe('ExtractStatusesPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractStatusesPipe();
    expect(pipe).toBeTruthy();
  });
});
