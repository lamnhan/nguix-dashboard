import { MediaExtractTypesPipe } from './media-extract-types.pipe';

describe('MediaExtractTypesPipe', () => {
  it('create an instance', () => {
    const pipe = new MediaExtractTypesPipe();
    expect(pipe).toBeTruthy();
  });
});
