import { MediaQueryFilterPipe } from './media-query-filter.pipe';

describe('MediaQueryFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MediaQueryFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
