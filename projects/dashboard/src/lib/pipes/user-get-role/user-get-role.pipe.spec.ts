import { UserGetRolePipe } from './user-get-role.pipe';

describe('UserGetRolePipe', () => {
  it('create an instance', () => {
    const pipe = new UserGetRolePipe();
    expect(pipe).toBeTruthy();
  });
});
