import { QueryFilterPipe } from './query-filter.pipe';

describe('QueryFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new QueryFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
