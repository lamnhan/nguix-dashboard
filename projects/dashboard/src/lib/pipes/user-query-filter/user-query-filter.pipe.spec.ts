import { UserQueryFilterPipe } from './user-query-filter.pipe';

describe('UserQueryFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UserQueryFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
