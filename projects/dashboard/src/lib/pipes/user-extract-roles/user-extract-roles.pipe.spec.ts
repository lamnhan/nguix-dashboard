import { UserExtractRolesPipe } from './user-extract-roles.pipe';

describe('UserExtractRolesPipe', () => {
  it('create an instance', () => {
    const pipe = new UserExtractRolesPipe();
    expect(pipe).toBeTruthy();
  });
});
