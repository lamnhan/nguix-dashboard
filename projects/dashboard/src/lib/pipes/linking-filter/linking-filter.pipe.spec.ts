import { LinkingFilterPipe } from './linking-filter.pipe';

describe('LinkingFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LinkingFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
