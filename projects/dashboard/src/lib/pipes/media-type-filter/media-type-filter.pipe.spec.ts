import { MediaTypeFilterPipe } from './media-type-filter.pipe';

describe('MediaTypeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MediaTypeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
